# axhub 배포 설정
# axhub backend가 package.json 의 "express" 의존성을 자동으로 감지해요.
# 아래 값은 기본값을 명시적으로 적어둔 것뿐.
name: express-axhub
runtime:
  port: 3000
  health_path: /api/health
build:
  install: npm install
  start: node index.js
