# axhub 배포 설정
# Remix 는 Vite preset 으로 잘못 매칭될 위험이 있어서 build/start 를 명시해요.
# remix-serve 가 SSR 핸들러를 Express 위에서 띄워줘요.
name: remix-axhub
runtime:
  port: 3000
  health_path: /
build:
  install: npm install
  build: npm run build
  start: npm start
