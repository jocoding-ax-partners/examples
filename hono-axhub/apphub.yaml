# axhub 배포 설정
# Hono 는 axhub 의 자동 preset 매칭 대상이 아니에요. build/start 명시 필수.
# tsc 가 src/ → dist/ 로 컴파일하고, @hono/node-server 가 PORT=3000 으로 떠요.
name: hono-axhub
runtime:
  port: 3000
  health_path: /api/health
build:
  install: npm install
  build: npm run build
  start: npm start
