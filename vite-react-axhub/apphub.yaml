# axhub 배포 설정
# axhub backend가 vite.config.* + "vite" 의존성을 자동으로 감지해서
# Vite preset(빌드 후 dist/ → nginx 정적 서빙) 으로 배포해요.
# 클라이언트 SPA 라서 server-side 환경변수(API_KEY 등) 없이 동작해요.
name: vite-react-axhub
runtime:
  port: 80
  health_path: /
