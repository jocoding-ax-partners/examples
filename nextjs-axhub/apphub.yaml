# axhub 배포 설정
# axhub backend가 next.config.* + "next" 의존성을 자동으로 감지해요.
# 아래 값은 기본값을 명시적으로 적어둔 것뿐이에요. 필요할 때만 바꿔요.
name: nextjs-axhub
runtime:
  port: 3000
  health_path: /
build:
  install: npm install
  build: npm run build
  start: npm start
